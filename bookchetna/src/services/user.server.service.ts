/**
 * User server service.
 * Currently the user route only returns session info,
 * so there is no direct database operation to extract.
 * Future user-related DB operations should be added here.
 */
